<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Mi primer página con Express
    body
        - var arreglo = [1,2,3,4,5];
        - for (var i = arreglo.length - 1; i >= 0; i--) 
            p!= '<h1>'+arreglo[i]+'</h1>'
            p Hola #{arreglo[i]}
        if true
            p. 
                Hola haz click en #[a(href="codigofacilito.com") este link] please
        each item in arreglo
            p= item
                case item
                    when 1
                        p El item era uno
                    when 2
                        p El item era dos
                    default
                        p El item no era ni uno, ni dos

        
        